# bam_filter
My bachelor thesis, BAM filter that keeps just the highest confidence reads based on rare k-mer presence, aimed at long reads, written completely in Rust!
## usage
```terminaloutput
$ bambam <INPUT_BAM_FILE> <OUTPUT_BAM_FILE> [options]
```

