# bam_filter
My bachelor thesis, BAM filter that keeps just the highest confidence reads, written in Rust and completely opensource!
## usage
```terminaloutput
$ bambam <INPUT_BAM_FILE> <OUTPUT_BAM_FILE> [options]
```

